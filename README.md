# Resumes
A Spring Boot-based application for generating ATS-compliant resumes from JSON input. Supports output in HTML, PDF, and DOCX formats with built-in ATS validation to ensure compatibility with Applicant Tracking Systems.
